import time

# from .utils import resource_path

class Application(object):
    def __init__(self):
        # self.file_path = resource_path('example.txt')
        return

    def gotoRegister(self):
        print(self)
        # from .ui.signup_ui import Ui_Register
        # register = Ui_Register()
        # print(register)
        # widget.setWindowTitle("Sign Up")
        # widget.setCurrentIndex(widget.currentIndex() + 1)
        # register.home.clicked.connect(self.__init__)
        # register.home.clicked.connect(lambda: widget.addWidget(home))
        # register.home.clicked.connect(lambda: widget.setCurrentIndex(widget.currentIndex() + 1))
